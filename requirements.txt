# ============================================================
# Distress Properties Scraper Service — Python dependencies
# ============================================================
# All versions are pinned for reproducibility. When upgrading, test
# locally before pushing to Railway because the deploy environment is
# the only place these run against real Supabase + scraper targets.
# ---- Web framework ----
fastapi==0.115.5
uvicorn[standard]==0.32.1
starlette==0.41.3
# ---- Configuration & data validation ----
pydantic==2.10.3
pydantic-settings==2.6.1
# ---- Email (Resend — access-approval notifications) ----
resend==2.4.0
# ---- Database client ----
supabase==2.10.0
postgrest==0.18.0
# ---- LLM (Anthropic Claude — NL search, summaries, extraction) ----
anthropic==0.40.0
# ---- HTTP client (used by all scrapers + geocoder) ----
httpx==0.27.2
# ---- HTML parsing ----
beautifulsoup4==4.12.3
lxml==5.3.0
# ---- PDF parsing (Hennepin & Ramsey sheriff scrapers) ----
pdfplumber==0.11.4
pypdfium2==4.30.0
# ---- Excel parsing (Washington sheriff monthly XLS files) ----
openpyxl==3.1.5
# ---- Browser automation fallback (Playwright) ----
# Included for future scrapers that may need JS-rendered pages.
# Currently unused but the Docker image installs Chromium for it.
playwright==1.49.0
# ---- Scheduler ----
apscheduler==3.10.4
# ---- Retry logic with exponential backoff ----
tenacity==9.0.0
# ---- Logging ----
loguru==0.7.2
# ---- Date/time utilities ----
python-dateutil==2.9.0.post0
# ---- Type stubs ----
typing-extensions>=4.12.0
